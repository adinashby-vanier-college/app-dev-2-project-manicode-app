import 'package:flutter/material.dart';

class ContactNailArtistPage extends StatelessWidget {
  const ContactNailArtistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Contact Your Nail Artist')),
      body: const Center(child: Text('This is the Contact Nail Artist Page')),
    );
  }
}